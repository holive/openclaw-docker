#!/bin/bash
# user-setup.sh - custom build-time hook
# this script runs during docker build as root
# add your custom toolchain installations here
#
# examples:
#   curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
#   pip install some-package
#   npm install -g some-tool

# add your custom setup below:
