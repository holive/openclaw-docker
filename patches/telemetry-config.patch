--- a/index.ts
+++ b/index.ts
@@ -6,7 +6,7 @@ export default {
   name: "OpenClaw Telemetry",
   description: "Captures tool calls, LLM usage, and message events to JSONL",
   register(api: OpenClawPluginApi) {
-    const svc = createTelemetryService();
+    const svc = createTelemetryService(api.pluginConfig as any);
     api.registerService(svc);

     api.on("before_tool_call", (evt, ctx) => {
--- a/src/service.ts
+++ b/src/service.ts
@@ -11,7 +11,7 @@ export type TelemetryService = OpenClawPluginService & {
   write: (evt: TelemetryEventInput) => void;
 };

-export function createTelemetryService(): TelemetryService {
+export function createTelemetryService(pluginConfig?: TelemetryConfig): TelemetryService {
   let fileWriter: TelemetryWriter | null = null;
   let syslogWriter: SyslogWriter | null = null;
   let unsubDiag: (() => void) | null = null;
@@ -40,7 +40,7 @@ export function createTelemetryService(): TelemetryService {
     id: "telemetry",
     write: writeEvent,
     async start(ctx) {
-      const cfg = ctx.config.plugins?.telemetry as TelemetryConfig | undefined;
+      const cfg = pluginConfig ?? ctx.config.plugins?.telemetry as TelemetryConfig | undefined;
       if (!cfg?.enabled) {
         return;
       }
